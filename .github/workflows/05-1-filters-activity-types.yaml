name: 05 - 1 - Event filters and Activity Types.
on: 
    pull_request:
        types: [opened, synchronize]
        branches:
            - main
jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: Echo the message
              run: echo "Running whenever a PR is opened or syncrhonized AND base branch is main."
        